import { Indonesian } from 'flatpickr/dist/l10n/id'

const date = {
  locale: Indonesian,
  altFormat: 'j F Y',
  altInput: true,
  maxDate: 'today',
}

const dateTime = {
  locale: Indonesian,
  altFormat: 'j F Y',
  altInput: true,
  maxDate: 'today',
}

export { date, dateTime }

export default date
